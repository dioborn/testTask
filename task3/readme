ТЗ
Дан текстовый файл размером 2ГБ. Напишите класс, реализующий интерфейс SeekableIterator, для чтения данного файла.

Реализация:
FileSeekableIterator - читает файл посимвольно. Скучно, неинтересно.

LogSeekableIterator - итератор для чтения логов. Можно задать разделитель для записей и значений  (по умолчанию \n и \t).
Размер потребляемой памяти зависит от максимальной длины запрашиваемой записи, а не от размера файла. 
В процессе обработки строит "карту" лога. Запоминает начальную позицию и длину записи для уже обработанных значений.
saveMap - методя для сохранения текущей карты лога
loadMap - метод для загрузки карты лога

В перспективе: добавить индекс и поиск значений по логу.